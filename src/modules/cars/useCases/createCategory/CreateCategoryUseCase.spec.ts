describe("Criar Categoria", () => {
  describe("SHOULD", () => {
    it("2 + 2 = 4", () => {
      expect(2 + 2).toBe(4);
    });
  });
  describe("SHOULD NOT", () => {
    it("2 + 3 = 4", () => {
      expect(2 + 3).not.toBe(4);
    });
  });
});
